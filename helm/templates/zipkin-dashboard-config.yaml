apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    grafana_dashboard: "1"
  name: zipkin-dashboard-config
  namespace: prometheus
data:
  zipkin.json: |-
{{ ( .Files.Get "dashboard/zipkin-prometheus.json") | indent 4 }}